// Two apps will live here: the driver capture PWA and the manager dashboard.
// Neither is started — capture screen layout is blocked on OI-28 (TYRE-12).
export function App() {
  return <h1>Tyre Platform</h1>;
}
